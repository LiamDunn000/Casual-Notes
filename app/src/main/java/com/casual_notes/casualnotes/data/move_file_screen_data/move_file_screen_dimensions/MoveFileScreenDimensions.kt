package com.casual_notes.casualnotes.data.move_file_screen_data.move_file_screen_dimensions

import com.casual_notes.casualnotes.data.shared_data.screenDimensions

class MoveFileScreenDimensions {

    // Move Files Screen Dimensions
    val moveFileScreenPadding = screenDimensions.screenSize * 0.01
    val moveFileScreenSpacing = screenDimensions.screenSize * 0.01

    // Move File Screen Top App Bar Dimensions
    val moveFileScreenTopAppBarHeight = screenDimensions.screenSize * 0.07f
    val moveFileScreenTopAppBarTopPadding = screenDimensions.screenSize * 0.02
    val moveFileScreenTopAppBarHorizontalPadding = screenDimensions.screenSize * 0.012

    // Folder Search Bar Dimensions
    val folderSearchBarWeight = 0.22f
    val folderSearchBarFontSize = screenDimensions.screenSize * 0.020
    val folderSearchBarIconSize = folderSearchBarFontSize

    // Move File Screen Button Dimensions
    val moveFileScreenTopAppBarButtonSize = screenDimensions.screenSize * 0.022
    val moveFileScreenTopAppBarButtonWeight = 0.032f

    // Files To Move List Dimensions
    val filesToMoveListSpacing = screenDimensions.screenSize * 0.01

    // Files To Move List Header Dimensions
    val filesToMoveListHeaderDecorativeLineHeight = screenDimensions.screenSize * 0.001
    val filesToMoveListHeaderDecorativeLineWeight = 1f
    val filesToMoveListHeaderFontSize = screenDimensions.screenSize * 0.02
    val filesToMoveListHeaderTextPadding = screenDimensions.screenSize * 0.01

    // Files To Move List Dimensions
    val filesToMoveListItemPadding = screenDimensions.screenSize * 0.009
    val filesToMoveListItemSpacing = screenDimensions.screenSize * 0.020
    val fileToMoveListNameFontSize = screenDimensions.screenSize * 0.023

    // Selected Destination Display Text Header Dimensions
    val selectedDestinationDisplayTextPadding = screenDimensions.screenSize * 0.01
    val selectedDestinationDisplayTextDecorativeLineHeight = screenDimensions.screenSize * 0.001
    val selectedDestinationDisplayTextHeaderDecorativeLineWeight = 1f
    val selectedDestinationDisplayTextHeaderFontSize = screenDimensions.screenSize * 0.02
    val selectedDestinationDisplayTextHeaderTextPadding = screenDimensions.screenSize * 0.01
    val selectedDestinationDisplayTextFontSize = screenDimensions.screenSize * 0.023

    // Destination List Dimensions
    val destinationListSpacing = screenDimensions.screenSize * 0.01
    val currentFolderDisplayFontSize = screenDimensions.screenSize * 0.023
    val destinationListMinimumSize = screenDimensions.screenSize * 0.13

    // Destination List Header Dimensions
    val destinationListHeaderDecorativeLineHeight = screenDimensions.screenSize * 0.001
    val destinationListHeaderDecorativeLineWeight = 1f
    val destinationListHeaderTextPadding = screenDimensions.screenSize * 0.01
    val destinationListHeaderFontSize = screenDimensions.screenSize * 0.02

    // Destination List Panel Dimensions
    val destinationListPanelPadding = screenDimensions.screenSize * 0.01
    val destinationListPanelButtonSize = screenDimensions.screenSize * 0.022
    val destinationListPanelButtonWeight = 0.032f
    val destinationListPanelFontSize = screenDimensions.screenSize * 0.020

    // Move File Button Dimensions
    val moveFileButtonSize = screenDimensions.screenSize * 0.062
    val moveFileButtonFontSize = screenDimensions.screenSize * 0.020

}